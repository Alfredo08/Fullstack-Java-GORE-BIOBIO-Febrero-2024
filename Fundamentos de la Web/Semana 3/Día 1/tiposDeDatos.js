// Boleano
let esVerdad = true;

// Strings
let nombre = "Alejandro";

// Números
let edad = 25;
let calificacion = 9.8;

// Nulo
let certificado = null;

// Indefinido
let apellido; //undefined

console.log(esVerdad, typeof(esVerdad));
console.log(nombre, typeof(nombre));
console.log(edad, typeof(edad));
console.log(calificacion, typeof(calificacion));
console.log(certificado, typeof(certificado));
console.log(apellido, typeof(apellido));
